# GitHub Copilot instructions

- In pull request descriptions, the first line must be `Fix GH-${ISSUE_NUMBER}` with `${ISSUE_NUMBER}` replaced by the corresponding issue number.
- Do not add a `Changes` section to pull request descriptions.
